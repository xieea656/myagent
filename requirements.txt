# AI Agent 依赖
# 安装：pip install -r requirements.txt

openai>=1.0.0          # LLM API 调用
prompt_toolkit>=3.0.0  # 终端 UI（底栏/输入框）
rich>=13.0.0           # 终端美化输出
tiktoken>=0.5.0        # 精确 token 计数
requests>=2.28.0       # HTTP 请求（AnySearch 等）
PyYAML>=6.0            # YAML 配置文件
python-dotenv>=1.0.0   # .env 环境变量加载